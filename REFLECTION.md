What did you learn from reading and commenting on this code?
Reading and commenting on this code helped me understand how higher-order functions like map(), filter(), and reduce() work with arrays of objects. I learned that map() is useful for creating a new array from certain values, filter() is useful for selecting specific items, and reduce() is useful for combining values into a total.

What was the most confusing part?
The most confusing part was understanding ...new Set(names), which this is not language based. It turns out that this means removes duplicates. I think it's very interesting and useful. 